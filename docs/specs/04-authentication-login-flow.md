# Authentication & Login Flow Specification

**Version:** 1.0 — LOCKED  
**Date:** January 2026  
**Status:** ✅ APPROVED

---

## Executive Summary

A minimal, clean authentication system using Supabase Auth. Designed for business owners who need to claim and manage their listings. Casual browsers do not need accounts.

### Key Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Auth provider | Supabase Auth | Already in stack, handles everything |
| Social login | Google only (MVP) | Simplest, covers most users |
| Apple login | Phase 2 | Requires more setup |
| UI pattern | Modal (primary), Page (fallback) | Less disruptive, modern UX |
| Magic links | Not for MVP | Nice-to-have, not essential |

---

## Who Needs to Log In?

```
USER AUTHENTICATION REQUIREMENTS
═══════════════════════════════════════════════════════════════

DOES NOT NEED LOGIN:
├── Browsing store listings
├── Browsing repair providers
├── Viewing business profiles
├── Clicking phone numbers
├── Visiting external websites
├── Using search
└── Reading content pages

NEEDS LOGIN:
├── Claiming a business listing
├── Editing business details
├── Accessing business dashboard
├── Viewing business analytics (future)
├── Responding to reviews (future)
└── Writing reviews (future)

═══════════════════════════════════════════════════════════════
```

---

## Login Trigger Points

```
WHERE LOGIN IS TRIGGERED
═══════════════════════════════════════════════════════════════

1. HEADER "Log in" LINK
   ├── Location: Top right, before any user menu
   ├── Always visible when logged out
   ├── Click → Opens login modal
   └── After login → Shows user menu

2. "FOR BUSINESS" DROPDOWN → "Business Dashboard"
   ├── Click when logged out → Opens login modal
   ├── After login → Redirects to /dashboard
   └── Return URL preserved

3. CLAIM FLOW (from Spec 03)
   ├── After verification code validated
   ├── If not logged in → Show account creation
   ├── If logged in → Link business to account
   └── Seamless integration

4. PROTECTED ROUTES (direct URL access)
   ├── /dashboard/*
   ├── /business/*/edit
   ├── If not logged in → Redirect to login
   └── After login → Return to original URL

═══════════════════════════════════════════════════════════════
```

---

## Authentication Flows

### Flow Overview

```
AUTHENTICATION FLOW DIAGRAM
═══════════════════════════════════════════════════════════════

                         ┌─────────────────┐
                         │  User clicks    │
                         │  "Log in"       │
                         └────────┬────────┘
                                  │
                                  ▼
                         ┌─────────────────┐
                         │  LOGIN MODAL    │
                         │  Opens          │
                         └────────┬────────┘
                                  │
                 ┌────────────────┼────────────────┐
                 │                │                │
                 ▼                ▼                ▼
        ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
        │   Email +    │ │   Google     │ │  Don't have  │
        │   Password   │ │   OAuth      │ │  account?    │
        └──────┬───────┘ └──────┬───────┘ └──────┬───────┘
               │                │                │
               ▼                │                ▼
        ┌──────────────┐       │         ┌──────────────┐
        │  Validate    │       │         │  SIGNUP      │
        │  credentials │       │         │  MODAL       │
        └──────┬───────┘       │         └──────────────┘
               │                │
               └────────┬───────┘
                        │
                        ▼
               ┌──────────────┐
               │   SUCCESS    │
               │  Close modal │
               │  Update UI   │
               └──────────────┘

═══════════════════════════════════════════════════════════════
```

---

## Login Modal

### Design Specification

```
LOGIN MODAL
═══════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│                                              [✕]            │
│                                                             │
│                      Welcome back                           │
│                                                             │
│         By continuing, you agree to our Terms of           │
│         Service and acknowledge our Privacy Policy.         │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Email address                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Password                                      👁   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│                                      Forgot password?       │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                      Log in                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ─────────────────────── or ───────────────────────────    │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  G     Continue with Google                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│         Don't have an account? Sign up                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════
```

### Modal Styling

```
MODAL STYLING
═══════════════════════════════════════════════════════════════

OVERLAY:
├── Background:     rgba(0, 0, 0, 0.5)
├── Position:       fixed, full screen
├── Z-index:        100
├── Click outside:  Closes modal
└── Transition:     opacity 150ms ease

MODAL CONTAINER:
├── Width:          400px (max)
├── Width mobile:   calc(100% - 32px)
├── Background:     #FFFFFF
├── Border radius:  12px
├── Padding:        32px
├── Box shadow:     0 20px 50px rgba(0, 0, 0, 0.2)
├── Position:       centered (flex)
└── Animation:      slideUp 200ms ease

CLOSE BUTTON:
├── Position:       absolute, top-right
├── Size:           32px
├── Icon:           ✕ (16px)
├── Color:          #6B7280
├── Hover:          #111827
└── Padding:        8px

HEADING:
├── Font size:      24px
├── Font weight:    700
├── Color:          #111827
├── Text align:     center
├── Margin bottom:  8px

SUBTEXT (Terms):
├── Font size:      13px
├── Color:          #6B7280
├── Text align:     center
├── Margin bottom:  24px
├── Links:          #e85d4c (secondary), underline on hover

INPUT FIELDS:
├── Height:         48px
├── Padding:        12px 16px
├── Border:         1px solid #E5E7EB
├── Border focus:   2px solid #e85d4c
├── Border radius:  8px
├── Font size:      16px
├── Width:          100%
├── Margin bottom:  16px
└── Placeholder:    #9CA3AF

PASSWORD TOGGLE:
├── Position:       absolute, right inside input
├── Icon:           👁 (eye) / 👁‍🗨 (eye-slash)
├── Size:           20px
├── Color:          #6B7280
└── Cursor:         pointer

FORGOT PASSWORD LINK:
├── Font size:      14px
├── Color:          #e85d4c (secondary)
├── Text align:     right
├── Margin bottom:  16px
└── Hover:          underline

PRIMARY BUTTON (Log in):
├── Height:         48px
├── Width:          100%
├── Background:     #DC2626 (red)
├── Color:          #FFFFFF
├── Font size:      16px
├── Font weight:    600
├── Border radius:  8px
├── Hover:          #B91C1C
├── Disabled:       opacity 0.5, cursor not-allowed
└── Margin bottom:  24px

DIVIDER:
├── Display:        flex, align-items center
├── Text:           "or"
├── Font size:      14px
├── Color:          #9CA3AF
├── Lines:          1px solid #E5E7EB, flex-grow
└── Gap:            16px

GOOGLE BUTTON:
├── Height:         48px
├── Width:          100%
├── Background:     #FFFFFF
├── Border:         1px solid #E5E7EB
├── Color:          #374151
├── Font size:      16px
├── Font weight:    500
├── Border radius:  8px
├── Hover:          Background #F9FAFB
├── Icon:           Google "G" logo, 20px
├── Icon margin:    12px right
└── Margin top:     16px

SIGNUP LINK:
├── Font size:      14px
├── Color:          #6B7280
├── Text align:     center
├── Margin top:     24px
└── Link color:     #e85d4c (secondary)

═══════════════════════════════════════════════════════════════
```

### Error States

```
LOGIN ERROR STATES
═══════════════════════════════════════════════════════════════

INVALID EMAIL FORMAT:
┌─────────────────────────────────────────────────────────────┐
│  Email address                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  notanemail                                         │   │ ← Red border
│  └─────────────────────────────────────────────────────┘   │
│  Please enter a valid email address                        │ ← Red text
└─────────────────────────────────────────────────────────────┘

WRONG PASSWORD:
┌─────────────────────────────────────────────────────────────┐
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ⚠️  Invalid email or password. Please try again.  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Email address                                              │
│  ┌─────────────────────────────────────────────────────┐   │
└─────────────────────────────────────────────────────────────┘

ERROR BANNER STYLING:
├── Background:     #FEF2F2 (red-50)
├── Border:         1px solid #FECACA (red-200)
├── Border radius:  8px
├── Padding:        12px 16px
├── Color:          #991B1B (red-800)
├── Font size:      14px
├── Icon:           ⚠️ warning
├── Margin bottom:  16px
└── Display:        flex, align-items center

FIELD ERROR TEXT:
├── Font size:      13px
├── Color:          #DC2626 (red)
├── Margin top:     4px
└── Display:        below input

INPUT ERROR STATE:
├── Border color:   #DC2626 (red)
└── Border width:   2px

═══════════════════════════════════════════════════════════════
```

---

## Signup Modal

```
SIGNUP MODAL
═══════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│                                              [✕]            │
│                                                             │
│                    Create your account                      │
│                                                             │
│         By continuing, you agree to our Terms of           │
│         Service and acknowledge our Privacy Policy.         │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  G     Continue with Google                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ─────────────────────── or ───────────────────────────    │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Full name                                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Email address                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Password                                      👁   │   │
│  └─────────────────────────────────────────────────────┘   │
│  At least 8 characters                                     │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                    Sign up                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│         Already have an account? Log in                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘

FIELD NOTES:
├── Full name: Required, min 2 characters
├── Email: Required, valid format, unique
├── Password: Required, min 8 characters
└── Google: Creates account if email doesn't exist

ON SIGNUP (Email):
├── Create user in Supabase Auth
├── Send verification email
├── Show "Check your email" screen
└── User clicks link → Email verified → Can log in

ON SIGNUP (Google):
├── Redirect to Google OAuth
├── On return: Create/link account
├── Auto-verified (Google confirmed email)
└── Redirect to return URL or dashboard

═══════════════════════════════════════════════════════════════
```

---

## Email Verification (After Signup)

```
EMAIL VERIFICATION SCREEN
═══════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│                                              [✕]            │
│                                                             │
│                          📧                                 │
│                                                             │
│                   Check your email                          │
│                                                             │
│         We've sent a verification link to:                 │
│         john.smith@example.com                             │
│                                                             │
│         Click the link in the email to verify              │
│         your account and start using [Brand Name].         │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│         Didn't receive the email?                          │
│         • Check your spam folder                           │
│         • Make sure the email is correct                   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Resend verification email              │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│         Wrong email? Start over                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘

RESEND BUTTON:
├── Style:          Secondary (outline)
├── Disabled for:   60 seconds after send
├── Show countdown: "Resend in 45s"
└── Max resends:    3 per session

═══════════════════════════════════════════════════════════════
```

---

## Forgot Password Flow

### Step 1: Enter Email

```
FORGOT PASSWORD — STEP 1
═══════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│                                              [✕]            │
│                                                             │
│                    Reset your password                      │
│                                                             │
│         Enter your email address and we'll send you        │
│         a link to reset your password.                     │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Email address                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  Send reset link                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│         Remember your password? Log in                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘

ON SUBMIT:
├── Validate email format
├── Call Supabase resetPasswordForEmail()
├── Always show success (security: don't reveal if email exists)
└── Redirect to "Check email" screen

═══════════════════════════════════════════════════════════════
```

### Step 2: Check Email Confirmation

```
FORGOT PASSWORD — STEP 2
═══════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│                                              [✕]            │
│                                                             │
│                          📧                                 │
│                                                             │
│                   Check your email                          │
│                                                             │
│         If an account exists for john@example.com,         │
│         we've sent a password reset link.                  │
│                                                             │
│         The link will expire in 1 hour.                    │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  Back to login                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════
```

### Step 3: Reset Password Page

```
RESET PASSWORD PAGE
═══════════════════════════════════════════════════════════════

URL: /auth/reset-password?token={token}

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                    [LOGO]                                   │
│                                                             │
│                  Set a new password                         │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  New password                                  👁   │   │
│  └─────────────────────────────────────────────────────┘   │
│  At least 8 characters                                     │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Confirm password                              👁   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  Reset password                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘

NOTE: This is a FULL PAGE, not modal (user clicks link from email)

ON SUBMIT:
├── Validate passwords match
├── Validate min 8 characters
├── Call Supabase updateUser({ password })
├── On success: Show success message
└── Redirect to login modal or dashboard

TOKEN VALIDATION:
├── If invalid/expired: Show error page
├── Error: "This link has expired. Please request a new one."
└── Link to "Request new reset link"

═══════════════════════════════════════════════════════════════
```

---

## Google OAuth Flow

```
GOOGLE OAUTH FLOW
═══════════════════════════════════════════════════════════════

USER CLICKS "CONTINUE WITH GOOGLE":
─────────────────────────────────────────────────────────────

1. Store return URL in sessionStorage
   └── returnUrl = current page or intended destination

2. Call Supabase signInWithOAuth
   └── supabase.auth.signInWithOAuth({
         provider: 'google',
         options: {
           redirectTo: `${origin}/auth/callback`
         }
       })

3. User redirected to Google
   └── Google consent screen appears

4. User approves, redirected back
   └── /auth/callback?code=...

5. Callback page handles token
   └── Supabase exchanges code for session

6. Redirect to return URL
   └── If new user: Go to dashboard
   └── If existing: Go to stored returnUrl

─────────────────────────────────────────────────────────────

CALLBACK PAGE: /auth/callback
─────────────────────────────────────────────────────────────

// app/auth/callback/page.tsx (or route.ts)

export default function AuthCallback() {
  useEffect(() => {
    const handleCallback = async () => {
      const { data, error } = await supabase.auth.getSession();
      
      if (error) {
        // Redirect to login with error
        router.push('/login?error=auth_failed');
        return;
      }
      
      // Get stored return URL
      const returnUrl = sessionStorage.getItem('returnUrl') || '/dashboard';
      sessionStorage.removeItem('returnUrl');
      
      router.push(returnUrl);
    };
    
    handleCallback();
  }, []);
  
  return <LoadingSpinner />;
}

═══════════════════════════════════════════════════════════════
```

---

## Logged In State

### Header Change When Logged In

```
HEADER — LOGGED OUT VS LOGGED IN
═══════════════════════════════════════════════════════════════

LOGGED OUT:
┌─────────────────────────────────────────────────────────────┐
│  [LOGO]    [Search Bar]     For Business ▼    Log in       │
└─────────────────────────────────────────────────────────────┘

LOGGED IN:
┌─────────────────────────────────────────────────────────────┐
│  [LOGO]    [Search Bar]     For Business ▼    [Avatar ▼]   │
└─────────────────────────────────────────────────────────────┘

AVATAR DROPDOWN (LOGGED IN):
┌─────────────────────────────────────────────────────────────┐
│  ┌────────────────────────────────────────────────────────┐ │
│  │  👤 John Smith                                        │ │
│  │  john.smith@example.com                               │ │
│  └────────────────────────────────────────────────────────┘ │
│  ─────────────────────────────────────────────────────────  │
│  📊  Dashboard                                              │
│  ⚙️  Account Settings                                       │
│  ─────────────────────────────────────────────────────────  │
│  🚪  Log out                                                │
└─────────────────────────────────────────────────────────────┘

AVATAR:
├── Size:           36px
├── Shape:          Circle
├── Default:        Initials on colored background
├── If Google:      Profile picture from Google
└── Border:         2px solid #E5E7EB

DROPDOWN ITEMS:
├── Dashboard:      → /dashboard
├── Account:        → /account/settings
└── Log out:        → Call supabase.auth.signOut()

═══════════════════════════════════════════════════════════════
```

---

## Protected Routes

```
PROTECTED ROUTES & MIDDLEWARE
═══════════════════════════════════════════════════════════════

PROTECTED ROUTES:
├── /dashboard
├── /dashboard/*
├── /business/*/edit
└── /account/*

MIDDLEWARE LOGIC:
─────────────────────────────────────────────────────────────

// middleware.ts

import { createMiddlewareClient } from '@supabase/auth-helpers-nextjs';
import { NextResponse } from 'next/server';

const protectedPaths = ['/dashboard', '/account', '/business'];

export async function middleware(req) {
  const res = NextResponse.next();
  const supabase = createMiddlewareClient({ req, res });
  
  const { data: { session } } = await supabase.auth.getSession();
  
  const isProtectedPath = protectedPaths.some(path => 
    req.nextUrl.pathname.startsWith(path)
  );
  
  if (isProtectedPath && !session) {
    // Store intended destination
    const redirectUrl = new URL('/login', req.url);
    redirectUrl.searchParams.set('returnTo', req.nextUrl.pathname);
    return NextResponse.redirect(redirectUrl);
  }
  
  return res;
}

═══════════════════════════════════════════════════════════════
```

---

## Integration with Business Claim Flow

```
CONNECTION TO SPEC 03: BUSINESS LISTING & CLAIM
═══════════════════════════════════════════════════════════════

SCENARIO: User claims business, not logged in
─────────────────────────────────────────────────────────────

1. User completes claim verification (code validated)

2. System checks: Is user logged in?
   └── NO → Show account creation form (from Spec 03)

3. Account creation form (embedded in claim flow):
   ┌─────────────────────────────────────────────────────┐
   │  Create your account                                │
   │                                                     │
   │  Email: info@store.co.uk (pre-filled, read-only)   │
   │  Name: [____________]                               │
   │  Password: [____________]                           │
   │                                                     │
   │  [ Complete Claim ]                                 │
   └─────────────────────────────────────────────────────┘

4. On submit:
   ├── Create Supabase Auth user
   ├── Link user to business (owner_id)
   ├── Log user in automatically
   └── Redirect to dashboard

─────────────────────────────────────────────────────────────

SCENARIO: User claims business, already logged in
─────────────────────────────────────────────────────────────

1. User completes claim verification (code validated)

2. System checks: Is user logged in?
   └── YES → Skip account creation

3. Directly link business to user:
   ├── Set business.owner_id = current user ID
   ├── Update business.status = 'claimed'
   └── Redirect to dashboard with success message

═══════════════════════════════════════════════════════════════
```

---

## Supabase Configuration

```
SUPABASE AUTH CONFIGURATION
═══════════════════════════════════════════════════════════════

REQUIRED SETUP IN SUPABASE DASHBOARD:
─────────────────────────────────────────────────────────────

1. AUTH SETTINGS:
   ├── Site URL: https://yourdomain.com
   ├── Redirect URLs: 
   │   ├── https://yourdomain.com/auth/callback
   │   └── https://yourdomain.com/auth/reset-password
   └── Email confirmations: ENABLED

2. EMAIL TEMPLATES (customize):
   ├── Confirm signup
   ├── Reset password
   └── Magic link (if enabled later)

3. GOOGLE OAUTH:
   ├── Enable Google provider
   ├── Add Client ID from Google Cloud Console
   ├── Add Client Secret
   └── Authorized redirect: Supabase callback URL

─────────────────────────────────────────────────────────────

GOOGLE CLOUD CONSOLE SETUP:
─────────────────────────────────────────────────────────────

1. Create OAuth 2.0 Client ID
2. Application type: Web application
3. Authorized JavaScript origins:
   ├── https://yourdomain.com
   └── http://localhost:3000 (dev)
4. Authorized redirect URIs:
   └── https://[project-ref].supabase.co/auth/v1/callback
5. Copy Client ID and Secret to Supabase

═══════════════════════════════════════════════════════════════
```

---

## Component File Structure

```
COMPONENT STRUCTURE
═══════════════════════════════════════════════════════════════

components/
└── auth/
    ├── AuthModal.tsx              # Container for login/signup modals
    ├── LoginForm.tsx              # Email + password form
    ├── SignupForm.tsx             # Registration form
    ├── ForgotPasswordForm.tsx     # Request reset link
    ├── ResetPasswordForm.tsx      # Set new password
    ├── EmailVerification.tsx      # "Check your email" screen
    ├── GoogleButton.tsx           # Google OAuth button
    ├── AuthDivider.tsx            # "or" divider
    ├── PasswordInput.tsx          # Password with toggle visibility
    └── UserMenu.tsx               # Logged-in user dropdown

lib/
└── auth/
    ├── supabase-client.ts         # Supabase client instance
    ├── auth-context.tsx           # Auth context provider
    ├── use-auth.ts                # useAuth hook
    └── protected-route.tsx        # HOC for protected pages

app/
├── auth/
│   ├── callback/
│   │   └── route.ts               # OAuth callback handler
│   ├── reset-password/
│   │   └── page.tsx               # Password reset page
│   └── verify/
│       └── page.tsx               # Email verification landing
├── login/
│   └── page.tsx                   # Fallback login page
└── middleware.ts                  # Route protection

hooks/
└── useAuthModal.ts                # Modal open/close state

═══════════════════════════════════════════════════════════════
```

---

## TypeScript Interfaces

```typescript
TYPESCRIPT INTERFACES
═══════════════════════════════════════════════════════════════

// types/auth.ts

import { User, Session } from '@supabase/supabase-js';

export interface AuthState {
  user: User | null;
  session: Session | null;
  isLoading: boolean;
  isAuthenticated: boolean;
}

export interface AuthContextType extends AuthState {
  signIn: (email: string, password: string) => Promise<AuthResult>;
  signUp: (email: string, password: string, name: string) => Promise<AuthResult>;
  signInWithGoogle: () => Promise<void>;
  signOut: () => Promise<void>;
  resetPassword: (email: string) => Promise<AuthResult>;
  updatePassword: (password: string) => Promise<AuthResult>;
}

export interface AuthResult {
  success: boolean;
  error?: string;
}

export interface LoginFormData {
  email: string;
  password: string;
}

export interface SignupFormData {
  name: string;
  email: string;
  password: string;
}

export interface UserProfile {
  id: string;
  email: string;
  name: string;
  avatar_url?: string;
  created_at: string;
}

═══════════════════════════════════════════════════════════════
```

---

## Auth Context Provider

```typescript
AUTH CONTEXT IMPLEMENTATION
═══════════════════════════════════════════════════════════════

// lib/auth/auth-context.tsx

'use client';

import { createContext, useContext, useEffect, useState } from 'react';
import { supabase } from './supabase-client';

const AuthContext = createContext<AuthContextType | undefined>(undefined);

export function AuthProvider({ children }: { children: React.ReactNode }) {
  const [state, setState] = useState<AuthState>({
    user: null,
    session: null,
    isLoading: true,
    isAuthenticated: false,
  });

  useEffect(() => {
    // Get initial session
    supabase.auth.getSession().then(({ data: { session } }) => {
      setState({
        user: session?.user ?? null,
        session,
        isLoading: false,
        isAuthenticated: !!session,
      });
    });

    // Listen for auth changes
    const { data: { subscription } } = supabase.auth.onAuthStateChange(
      (_event, session) => {
        setState({
          user: session?.user ?? null,
          session,
          isLoading: false,
          isAuthenticated: !!session,
        });
      }
    );

    return () => subscription.unsubscribe();
  }, []);

  const signIn = async (email: string, password: string) => {
    const { error } = await supabase.auth.signInWithPassword({
      email,
      password,
    });
    return { success: !error, error: error?.message };
  };

  const signUp = async (email: string, password: string, name: string) => {
    const { error } = await supabase.auth.signUp({
      email,
      password,
      options: {
        data: { name },
      },
    });
    return { success: !error, error: error?.message };
  };

  const signInWithGoogle = async () => {
    await supabase.auth.signInWithOAuth({
      provider: 'google',
      options: {
        redirectTo: `${window.location.origin}/auth/callback`,
      },
    });
  };

  const signOut = async () => {
    await supabase.auth.signOut();
  };

  const resetPassword = async (email: string) => {
    const { error } = await supabase.auth.resetPasswordForEmail(email, {
      redirectTo: `${window.location.origin}/auth/reset-password`,
    });
    return { success: !error, error: error?.message };
  };

  const updatePassword = async (password: string) => {
    const { error } = await supabase.auth.updateUser({ password });
    return { success: !error, error: error?.message };
  };

  return (
    <AuthContext.Provider
      value={{
        ...state,
        signIn,
        signUp,
        signInWithGoogle,
        signOut,
        resetPassword,
        updatePassword,
      }}
    >
      {children}
    </AuthContext.Provider>
  );
}

export const useAuth = () => {
  const context = useContext(AuthContext);
  if (!context) {
    throw new Error('useAuth must be used within AuthProvider');
  }
  return context;
};

═══════════════════════════════════════════════════════════════
```

---

## Validation Rules

```
VALIDATION RULES
═══════════════════════════════════════════════════════════════

EMAIL:
├── Required
├── Valid email format (regex)
└── Error: "Please enter a valid email address"

PASSWORD (Login):
├── Required
└── Error: "Password is required"

PASSWORD (Signup/Reset):
├── Required
├── Minimum 8 characters
├── Error: "Password must be at least 8 characters"

CONFIRM PASSWORD:
├── Required
├── Must match password
└── Error: "Passwords do not match"

NAME:
├── Required
├── Minimum 2 characters
└── Error: "Please enter your name"

EMAIL REGEX:
/^[^\s@]+@[^\s@]+\.[^\s@]+$/

═══════════════════════════════════════════════════════════════
```

---

## Accessibility

```
ACCESSIBILITY REQUIREMENTS
═══════════════════════════════════════════════════════════════

MODAL:
├── Focus trapped within modal when open
├── ESC key closes modal
├── aria-modal="true"
├── aria-labelledby pointing to heading
├── Return focus to trigger element on close

FORMS:
├── All inputs have associated <label>
├── Error messages linked with aria-describedby
├── Required fields have aria-required="true"
├── Invalid fields have aria-invalid="true"
├── Submit on Enter key

BUTTONS:
├── Clear focus states
├── Disabled state announced
├── Loading state announced (aria-busy)

PASSWORD TOGGLE:
├── aria-label="Show password" / "Hide password"
├── Toggle announces state change

KEYBOARD:
├── Tab navigates through form fields
├── Enter submits form
├── ESC closes modal
└── All interactive elements focusable

═══════════════════════════════════════════════════════════════
```

---

## Mobile Considerations

```
MOBILE ADAPTATIONS
═══════════════════════════════════════════════════════════════

MODAL ON MOBILE:
├── Full width (minus 16px padding each side)
├── Vertically centered or bottom sheet style
├── Larger touch targets (48px minimum)
└── Input font size 16px (prevents iOS zoom)

INPUTS:
├── type="email" → Email keyboard
├── type="password" → Secure entry
├── autocomplete="email" / "current-password" / "new-password"
└── inputmode where appropriate

GOOGLE BUTTON:
├── Full width
├── Height: 52px (larger touch target)
└── Clear tap state

CLOSE BUTTON:
├── Size: 44px touch target (even if icon is smaller)
└── Position: Top right with adequate padding

═══════════════════════════════════════════════════════════════
```

---

## Security Considerations

```
SECURITY MEASURES
═══════════════════════════════════════════════════════════════

PASSWORD:
├── Never logged or stored in plain text
├── Minimum 8 characters enforced
├── Supabase handles hashing (bcrypt)
└── No password hints or security questions

PASSWORD RESET:
├── Don't reveal if email exists
├── Token expires in 1 hour
├── Single use tokens
└── HTTPS only

SESSION:
├── JWT tokens managed by Supabase
├── HttpOnly cookies for refresh token
├── Short-lived access tokens (1 hour default)
└── Automatic refresh

RATE LIMITING:
├── Login attempts: Handled by Supabase
├── Password reset: Max 3 per hour per email
└── Signup: Handled by Supabase

OAUTH:
├── State parameter to prevent CSRF
├── PKCE flow for added security
└── Validate redirect URLs

═══════════════════════════════════════════════════════════════
```

---

## Implementation Checklist

### Phase 1: Core Authentication (MVP)
- [ ] Supabase Auth configuration
- [ ] AuthProvider context
- [ ] Login modal (email/password)
- [ ] Signup modal
- [ ] Email verification flow
- [ ] Forgot password flow
- [ ] Reset password page
- [ ] Google OAuth integration
- [ ] Middleware for protected routes
- [ ] Header login/user menu
- [ ] Basic error handling

### Phase 2: Enhancements
- [ ] Apple Sign-in
- [ ] Magic links (passwordless)
- [ ] Remember me checkbox
- [ ] Session management UI
- [ ] Account settings page
- [ ] Email change flow
- [ ] Delete account flow

### Phase 3: Integration
- [ ] Connect to business claim flow
- [ ] Business dashboard access
- [ ] Analytics tracking (login events)
- [ ] Rate limiting fine-tuning

---

## Error Messages Reference

```
ERROR MESSAGES
═══════════════════════════════════════════════════════════════

LOGIN ERRORS:
├── "Invalid email or password. Please try again."
├── "Too many login attempts. Please try again later."
├── "Your account has been disabled. Contact support."
└── "Please verify your email before logging in."

SIGNUP ERRORS:
├── "An account with this email already exists."
├── "Password must be at least 8 characters."
├── "Please enter a valid email address."
└── "Unable to create account. Please try again."

PASSWORD RESET ERRORS:
├── "Unable to send reset link. Please try again."
├── "This reset link has expired. Please request a new one."
├── "Passwords do not match."
└── "Password must be at least 8 characters."

GOOGLE AUTH ERRORS:
├── "Unable to connect to Google. Please try again."
├── "Google authentication was cancelled."
└── "Unable to complete sign-in. Please try again."

GENERAL:
├── "Something went wrong. Please try again."
└── "No internet connection. Please check your network."

═══════════════════════════════════════════════════════════════
```

---

**END OF AUTHENTICATION & LOGIN FLOW SPECIFICATION**

═══════════════════════════════════════════════════════════════
Document Status: ✅ LOCKED
Version: 1.0
Approved: January 2026
Next: Implementation Phase 1
═══════════════════════════════════════════════════════════════
